{
  "name": "webpack",
  "version": "1.0.0",
  "description": "build ? -> multiple web requests to server so latency is introduces perfomance can be better if js into 1 file, css into 1 file  etc",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack-dev-server -d"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.11.4",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.9.0",
    "jshint": "^2.9.2",
    "jshint-loader": "^0.8.3",
    "node-libs-browser": "^1.0.0",
    "strip-loader": "^0.1.2",
    "webpack": "^1.13.1"
  }
}
